[package]
name = "security"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
regex = "1"
sha2 = "0.10"
once_cell = "1"
dirs-next = "2"

[dev-dependencies]
tempfile = "3"
