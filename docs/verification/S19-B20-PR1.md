# S19-B20-PR1 Verification

## GOAL達成チェックリスト
- [x] i18n SSOT docs を `docs/specs/i18n/**` に整備（policy/keys/units/schema/required_keys/README）。
- [x] `apps/desktop/i18n/resources/{ja,en}.json` を正本として追加し、主要導線キーを100%充足。
- [x] `core/serialize/tests/spec_ssot_lint.rs` に i18n SSOT lint を追加（schema検証、required_keys充足、`UI.` key形式、空値禁止、危険制御文字検知）。
- [x] 欠落/重複/不正schemaを CI テストで必ず検知できる状態にした。
- [x] SSOT lint が通るケースを確認（`cargo test -p craftcad_serialize --tests` で pass）。
- [x] SSOT lint が落ちるケースを確認（実装途中で schema 不一致により `ssot_i18n_resources_validate_and_cover_required_keys` が fail し、修正後 pass）。

## 変更ファイル一覧
### 追加
- `docs/specs/i18n/README.md`
- `docs/specs/i18n/keys.md`
- `docs/specs/i18n/units.md`
- `docs/specs/i18n/i18n.schema.json`
- `docs/specs/i18n/required_keys.json`
- `apps/desktop/i18n/resources/ja.json`
- `apps/desktop/i18n/resources/en.json`
- `docs/verification/S19-B20-PR1.md`

### 編集
- `docs/specs/i18n/policy.md`（旧3行の簡易版から品質契約へ拡張）
- `core/serialize/tests/spec_ssot_lint.rs`（i18n SSOT lint追加）

## SSOT整合（B20-03/B20-04）
- B20-03（i18n SSOT固定）:
  - key命名規約・placeholder・fallback・危険制御文字・limitsを `policy.md` / `keys.md` / `units.md` に明文化。
  - required key の機械可読正本を `required_keys.json` に固定。
- B20-04（CI品質ゲート）:
  - `spec_ssot_lint` テストで schema + required coverage + key format + dangerous controls を検証。
  - `scripts/ci/run_all.sh` の `ssot_lint` ステップで pass を確認。

## history evidence（Step0）
実行コマンドと要点:
- `git status --porcelain=v1`:
  - 変更なし（clean）で開始。
- `git fetch --all --prune`:
  - fetch成功。
- `git checkout -b feature/s19-b20-pr1-001`:
  - 新規ブランチ作成成功。
- `git log -n 30 --oneline`:
  - 先頭: `eb482cb Merge pull request #95 ...`
- `git log --graph --decorate --oneline -n 50`:
  - HEAD は `feature/s19-b20-pr1-001` / `work` と同一点 `eb482cb`。
- `git branch -vv`:
  - `feature/s19-b20-pr1-001 eb482cb ...`
- `git show HEAD`:
  - HEAD差分は S18 security gate 系（15 files changed）。
- `git grep -n -F "spec_ssot_lint" .`:
  - 既存 lint 実体: `core/serialize/tests/spec_ssot_lint.rs`。
- `git grep -n -F "ReasonCatalog" core docs`:
  - diagnostics 周辺実装と lint 参照を確認。

## allowlist自己監査
- 変更は以下 allowed paths のみ:
  - `docs/**`
  - `core/**`
  - `apps/desktop/**`
- `.github/workflows/**` と削除操作は実施していない。

## テスト実行結果（要点）
- `cargo fmt --manifest-path core/Cargo.toml --all`
  - 失敗（既存ファイル `core/crates/ssot_lint/tests/golden_datasets.rs` に conflict marker があり、今回変更範囲外の環境起因）。
- `cargo test --manifest-path core/Cargo.toml -p craftcad_serialize --tests`
  - 最終実行で全テスト pass（13/13 in `spec_ssot_lint` を含む）。
- `scripts/ci/run_all.sh`
  - `rust_fmt` は同じ既存 conflict marker で fail。
  - それ以外は `ssot_lint` を含め pass（最後に artifacts 収集）。
