{
  "id": "a3_landscape_v1",
  "sheet": { "size": "A3", "width_mm": 420, "height_mm": 297, "margin_mm": 12 },
  "title_block": {
    "project_title": "${project_title}",
    "schema_version": "${schema_version}",
    "app_version": "${app_version}",
    "unit": "${unit}",
    "scale": "${scale}"
  }
}
